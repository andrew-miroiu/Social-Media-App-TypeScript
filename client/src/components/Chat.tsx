import { useEffect, useState } from "react"
import type { User } from "@supabase/supabase-js"

export default function Chat({currentUserId, userId} : {currentUserId: string; userId: string}) {
   
    
    return(
        
    )
}